{
  "name": "@eip/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared API contract types; the single source of truth for the client (ADR-001)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "sync": "node ./scripts/sync-openapi.mjs",
    "lint": "echo \"@eip/contracts: types only\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"@eip/contracts: no tests\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
